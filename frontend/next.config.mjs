/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.easyfrontend.com', "images.remotePatterns", 'plus.unsplash.com', 'localhost'],
    },
};

export default nextConfig;
